type Callback = (fps: number) => void;
declare class FpsCounter {
    private _lastLoop;
    private _fps;
    private _isStart;
    private _callbacks;
    private _tick;
    get fps(): number;
    start(): void;
    stop(): void;
    onTick(callback: Callback): () => void;
}
export { FpsCounter };
